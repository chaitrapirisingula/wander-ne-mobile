# Copy to .env for local development. For EAS / Play Store / App Store builds,
# add the same variables in Expo: Project → Environment variables, or:
#   eas env:create --name FIREBASE_API_KEY --value "..." --environment production

FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_DATABASE_URL=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=
MAPBOX_ACCESS_TOKEN=
